# Agent Notes

## GitHub Pages

This repository has GitHub Pages enabled.

- Publishing source: `main` branch
- Publishing folder: `/docs`

When adding or updating the site content, keep the GitHub Pages entry files and generated/static site assets under `docs/`. Do not move the site to the repository root or another branch unless the GitHub Pages settings are updated at the same time.
